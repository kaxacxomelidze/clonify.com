# Backend API origin (no trailing slash). Used by the browser for /api/* calls.
# Local: http://localhost:5000
# Production (same host via nginx): https://clonyfy.com
VITE_API_BASE_URL=http://localhost:5000
