// This schema was auto-generated by clonyfy based on detected form endpoints.
// Run `npx prisma db push` to apply.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

{{#each models}}
model {{name}} {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
{{fields}}
}

{{/each}}
{{#unless models.length}}
// No form endpoints detected — add your own models here.
{{/unless}}
