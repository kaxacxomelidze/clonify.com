SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# Direct Postgres URI for schema migrations (npm run db:migrate).
# Supabase → Project Settings → Database → Connection string → URI
SUPABASE_DB_URL=

# Required for /admin. Use a long random value and set the same value in your
# hosting provider's environment variables.
ADMIN_PASSWORD=

# Strongly recommended in production.
PASSWORD_PEPPER=
SHARE_PASSWORD_PEPPER=

# Optional.
PORT=5000

# ── Vercel Backend (separate project from Frontend) ───────────────────────────
# Auto-detected when VERCEL=1 / VERCEL_ENV is set. Prefer setting these explicitly:
# CLONYFY_QUALITY=1
# CLONYFY_SERVERLESS=1
# CLONYFY_HOSTED=1
# Do NOT set CLONYFY_PREFER_LIVE_MEDIA=1 (hotlinks CDNs → font CORS). Default is local /_assets.
# Do NOT set CLONYFY_SCROLL_REVEAL=1 (re-hides text; Save As level keeps content visible).
# Do NOT set CLONYFY_FAST_CLONE=1 unless you need emergency low-RAM mode (hurts fidelity).
# CLONYFY_SERVERLESS_MAX_PAGES=5
# CLONYFY_CLONE_DEADLINE_MS=280000
# APP_URL=https://api.clonyfy.com
# FRONTEND_URL=https://www.clonyfy.com
# Requires Vercel Pro (or Fluid) for maxDuration 300s. Durable files → Supabase Storage.

# Full-site crawl budget for Scale "Select all pages" (dedicated Node hosts).
# Only when CLONYFY_SERVERLESS=1: CLONYFY_SERVERLESS_FULL_SITE_MAX_PAGES also applies.
CLONYFY_FULL_SITE_MAX_PAGES=10000
CLONYFY_FULL_SITE_DEPTH=256
# Max/full-site wall-clock before salvage (dedicated hosts default 4h; Vercel stays at clone deadline).
# CLONYFY_FULL_SITE_DEADLINE_MS=14400000
# Sitemap URLs seeded after homepage (full-site default 2000 on dedicated hosts).
# CLONYFY_FULL_SITE_SITEMAP_CAP=2000
# Per-page Playwright timeout for non-home pages in full-site mode (default 210s).
# CLONYFY_FULL_SITE_PAGE_TIMEOUT_MS=210000
# Hosted clone speed: parallel pages (1–4). Free/Starter / serverless default 1.
# CLONYFY_CLONE_CONCURRENCY=1
# Soften concurrency/deadlines/exports on low RAM. Auto-on for Render/Vercel unless =0.
# CLONYFY_LOW_MEMORY=1
# Wall-clock limit in ms before salvage (Vercel quality default ~280s; Render low-memory ~12m).
# CLONYFY_CLONE_DEADLINE_MS=720000
# Quality-first is default. Only set FAST_CLONE=1 for emergency low-cost runs.
# CLONYFY_QUALITY=1
# CLONYFY_FAST_CLONE=0
# Opt-in CDN hotlink only (breaks fonts via CORS). Leave unset for local /_assets clone.
# CLONYFY_PREFER_LIVE_MEDIA=0
# Skip Next.js project generation (preview uses captured HTML). Auto-on for hosted.
# CLONYFY_SKIP_NEXT=1
# CLONYFY_SERVERLESS_MAX_PAGES=5
# CLONYFY_SERVERLESS_FULL_SITE_MAX_PAGES=5
# Override clone output root (serverless defaults to os.tmpdir()/clonyfy-output).
# CLONYFY_OUTPUT_DIR=

# Backend public URL (OAuth callback, emails that hit the API).
# On Render, RENDER_EXTERNAL_URL is used if APP_URL is unset.
APP_URL=http://localhost:5000
# Frontend app origin (CORS + post-login / OAuth browser redirects).
FRONTEND_URL=http://localhost:8080
# Comma-separated extra browser origins allowed for CORS (www + apex in production).
# CORS_ORIGINS=https://www.clonyfy.com,https://clonyfy.com,https://clonyfy-haq5.vercel.app
PUBLIC_APP_URL=
# Optional: force share links onto a specific host/subdomain.
SHARE_BASE_URL=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=
# Google OAuth (client ID/secret) is NOT an env var -- configure it from the
# /admin panel's Settings section instead; it's stored in the database.

# Figma Community plugin (production)
# After publishing Clonyfy Import, set the listing URL so the Export Figma modal
# shows "Install Clonyfy Import" for customers (no local figma-plugin folder).
# Example: https://www.figma.com/community/plugin/1677522506571131225/Clonyfy-Import
FIGMA_COMMUNITY_PLUGIN_URL=https://www.figma.com/community/plugin/1677522506571131225/Clonyfy-Import
# Until published: customers use Download SVG for Figma Web; Desktop plugin is for
# Development installs / internal testing only. See figma-plugin/PUBLISH.md.
